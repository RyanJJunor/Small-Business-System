package logic.enums;

/**
 * Used to determine which message to display in alerts
 *
 * @author Ryan Junor
 */
public enum DatabaseMessageType {
    USERADDED, USERNOTADDED, USERREMOVED, USERNOTREMOVED, PASSWORDCHANGED, PASSWORDNOTCHANGED, PASSWORDRESET, PASSWORDNOTRESET;
}
