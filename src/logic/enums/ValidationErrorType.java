package logic.enums;

/**
 * Used to determine which error message to display
 *
 * @author Ryan Junor
 */
public enum ValidationErrorType {
    LOGIN, INVALIDLOGIN, CUSTOMER, DUPLICATE, QUANTITY, EMPTY, NOITEMS, FNAME, LNAME, ENUMBER, ADMIN, STOCK,
    EMAIL, PHONE, LINE1, LINE2, CITY, COUNTY, POSTCODE, NOORDER, REPORT, NOREPORT, NOITEM, NOTVALID, INVALIDPASS,
    PASSNOMATCH, OLDPASS;

}
